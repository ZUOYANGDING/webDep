<%include partials/head%>
<h1>Here is all campgrounds</h1>
<a href="/campgrounds/new">Add new campground</a>
<% campgrounds.forEach(function (campground) {%>
    <div>
        <h4><%= campground.name%></h4>
        <img src="<%= campground.image%>">
    </div>
<%}); %>
<%include partials/tail%>